server:
  port: ${PORT:8084}

spring:
  application:
    name: order-service

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.gateway: DEBUG
    org.springframework: INFO