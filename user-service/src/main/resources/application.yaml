server:
  port: ${PORT:8086}

spring:
  application:
    name: user-service

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.gateway: DEBUG
    org.springframework: INFO