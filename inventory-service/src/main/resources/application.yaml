server:
  port: ${PORT:8082}

spring:
  application:
    name: inventory-service

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.example.gateway: DEBUG
    org.springframework: INFO